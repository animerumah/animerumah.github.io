import { sidebarNewAnime } from 'animerumah/helpers/sidebar-new-anime';
import { module, test } from 'qunit';

module('Unit | Helper | sidebar new anime');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sidebarNewAnime([42]);
  assert.ok(result);
});
