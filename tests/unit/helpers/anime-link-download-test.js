import { animeLinkDownload } from 'animerumah/helpers/anime-link-download';
import { module, test } from 'qunit';

module('Unit | Helper | anime link download');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = animeLinkDownload([42]);
  assert.ok(result);
});
